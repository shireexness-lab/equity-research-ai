# ดึงข้อมูลบน MacBook แล้วส่งขึ้นเว็บ

> อ่านเอกสารนี้เมื่อ **กดคัดกรองบนเว็บแล้วได้ 0 ตัว** แต่รันบน MacBook แล้วได้ครบ

---

## ทำไมเว็บดึงข้อมูลไม่ได้

Yahoo Finance บล็อกคำขอที่มาจากศูนย์ข้อมูล ซึ่งรวมถึง Streamlit Cloud

เหตุผลของ Yahoo: เครื่องบ้านดูเหมือนคนใช้งานจริง เครื่องในศูนย์ข้อมูลดูเหมือนหุ่นยนต์ที่มาดูดข้อมูล
ยิ่ง Streamlit Cloud ใช้ IP ร่วมกับผู้ใช้ทั่วโลก ถ้ามีคนอื่นยิงถี่ เราก็โดนหางเลขไปด้วย

**นี่ไม่ใช่บั๊กของโปรแกรม และแก้ที่โค้ดไม่ได้**

---

## วิธีแก้ — ให้ Mac เป็นคนดึง เว็บเป็นคนอ่าน

```
[ MacBook ]  ดึงจาก Yahoo ──► data/snapshots/  ──► git push
                                                      │
                                                      ▼
[ เว็บ / iPhone ]  กดปุ่ม ⚡ ──────────────► อ่านไฟล์  ──► ได้ผลใน 2 วินาที
```

เว็บไม่ต้องคุยกับ Yahoo เลย จึงไม่มีทางโดนบล็อก

**ไม่ต้องใช้กุญแจ ไม่ต้องสมัครบริการอะไรเพิ่ม** — ไฟล์เดินทางไปกับโค้ดผ่าน git ที่มีอยู่แล้ว

---

## ขั้นตอน (2 คำสั่ง)

### 1. ดึงข้อมูลบน Mac

```bash
eq
python3 tools_snapshot_build.py --thai
```

จะเห็นแถบความคืบหน้าแบบนี้:

```
คัดกรอง 866 ตัว  (ชื่อชุดข้อมูล: thai-ทั้งหมด-ทุกกลุ่ม-866)
----------------------------------------------------------
  [██████████████████████████████████] 100.0%

  ดึงสำเร็จ : 812 / 866 ตัว  (94%)
  ใช้เวลา   : 4.2 นาที

  บันทึก    : 61.3 KB
    ในโปรเจกต์   : สำเร็จ   (data/snapshots/)
    เครื่องนี้    : สำเร็จ
```

### 2. ส่งขึ้นเว็บ

โปรแกรมจะพิมพ์คำสั่งให้คัดลอกไปวางได้เลย:

```bash
git add data/snapshots
git commit -m "อัปเดตข้อมูลคัดกรอง"
git push
```

รอ 1–2 นาทีให้ Streamlit โหลดใหม่ แล้วกดปุ่ม **⚡ ใช้ข้อมูลที่บันทึกไว้** ได้ทั้งบนเว็บและมือถือ

---

## ตัวเลือกอื่น ๆ

```bash
python3 tools_snapshot_build.py --thai --limit 30       # ทดลอง 30 ตัวก่อน (~1 นาที)
python3 tools_snapshot_build.py --thai --market SET     # เฉพาะ SET ไม่เอา mai
python3 tools_snapshot_build.py --thai --industry "เทคโนโลยี"
python3 tools_snapshot_build.py --us                    # หุ้นสหรัฐยอดนิยม 39 ตัว
python3 tools_snapshot_build.py --us-all                # หุ้นสหรัฐทั้งตลาด (ผ่าน SEC)
```

> **สำคัญ** — ขอบเขตต้องตรงกับที่เลือกในหน้าเว็บ
> ถ้าบน Mac รัน `--market SET` แต่บนเว็บเลือก "ทั้งหมด" จะเป็นคนละชุดข้อมูล ปุ่ม ⚡ จะไม่ขึ้น
> วิธีง่ายที่สุดคือรัน `--thai` เฉย ๆ แล้วบนเว็บเลือก "ทั้งหมด" + "ทุกกลุ่ม"

ดูว่ามีอะไรเก็บไว้บ้าง:

```bash
python3 snapshot.py --list
```

---

## ระบบเลือกข้อมูลชุดไหนมาแสดง

มีที่เก็บ 3 ที่ ระบบจะอ่านทุกที่แล้ว **เลือกอันที่ใหม่ที่สุด**

| ที่เก็บ | ใครเขียน | รอดเมื่อ Streamlit รีสตาร์ท |
|---|---|---|
| `data/snapshots/` (ในโปรเจกต์) | Mac ผ่าน git | ✅ |
| `cache/snapshots/` (ในเครื่อง) | เว็บและ Mac | ❌ |
| Google Drive | ถ้าตั้งค่าไว้ | ✅ |

ที่ไม่ไล่ตามลำดับเพราะจะเจอกรณีนี้: ดึงบน Mac เมื่อวาน → วันนี้ push ของใหม่ → เปิดเว็บบน Mac แล้วเจอของเมื่อวานก่อน ผู้ใช้จะไม่รู้เลยว่ากำลังดูข้อมูลเก่า

ปุ่ม ⚡ จะบอกอายุข้อมูลไว้เสมอ (เช่น *"812 ตัว · 3 ชม.ที่แล้ว"*)

---

## ข้อควรระวัง

**ข้อมูลที่บันทึกคือภาพนิ่งของวันที่ดึง** ราคาหุ้นเปลี่ยนทุกวัน
ปุ่ม ⚡ เหมาะกับตอนสำรวจและปรับเกณฑ์คัดกรองไปมา
ถ้าจะตัดสินใจลงทุนจริง ให้ดึงใหม่บน Mac ก่อนเสมอ

**ไฟล์นี้อยู่ใน repo ที่เป็น public** — เนื้อหาคือราคาปิดและอัตราส่วนของบริษัทจดทะเบียน
ซึ่งเป็นข้อมูลสาธารณะที่ใครก็ดูได้จากเว็บ SET ไม่มีข้อมูลส่วนตัวของอาจารย์อยู่ในนั้นเลย

ถ้าไม่อยากให้คนอื่นเห็นว่าดึงข้อมูลอะไรไว้ มี 2 ทาง:

1. เปลี่ยน repo เป็น private (Streamlit Community Cloud รองรับ — ผมช่วยตรวจสอบขั้นตอนให้ได้)
2. ใช้ Google Drive แทน ตามคู่มือ `19_เก็บข้อมูลไว้ที่_Google_Drive.md`

---

## อัปเดตอัตโนมัติทุกวัน โดยไม่ต้องเปิดเครื่อง

ติดตั้งไว้ให้แล้วที่ `.github/workflows/update-snapshot.yml`

**เครื่องของ GitHub เป็นคนรันให้** — Mac ปิดอยู่ก็ทำงาน

```
ทุกวันจันทร์-ศุกร์ 17:30 น. (หลังตลาดไทยปิด)
        │
        ▼
[ เครื่องของ GitHub ]  ดึงข้อมูล ──► commit กลับเข้า repo เอง
                                          │
                                          ▼
              [ เว็บ / iPhone ]  เห็นข้อมูลใหม่โดยอัตโนมัติ
```

| | รายละเอียด |
|---|---|
| เวลา | 17:30 น. เวลาไทย จันทร์–ศุกร์ (ตลาดปิด 16:30 จึงได้ราคาปิดจริง) |
| ค่าใช้จ่าย | ฟรี (repo แบบ public ใช้ GitHub Actions ได้ไม่จำกัด) |
| กุญแจที่ต้องเก็บ | **ไม่มี** — GitHub สร้าง token ให้เองตอนรัน แล้วทิ้งทันทีที่จบ |
| ถ้าดึงไม่สำเร็จ | ไม่ commit ทับของเดิม และเขียนสาเหตุไว้ในหน้าสรุป |

### เปิดใช้งาน

หลัง `git push` ครั้งแรก ให้เข้าไปเปิดสิทธิ์ให้ workflow เขียน repo ได้:

1. เปิด repo บน GitHub → **Settings** → **Actions** → **General**
2. เลื่อนลงไปที่ **Workflow permissions**
3. เลือก **Read and write permissions** → **Save**

### ทดลองรันทันที (ไม่ต้องรอถึง 17:30)

1. เปิด repo บน GitHub → แท็บ **Actions**
2. เลือก **อัปเดตข้อมูลคัดกรองรายวัน** ทางซ้าย
3. กด **Run workflow** → เลือก `thai` → **Run workflow**
4. รอ 5–10 นาที แล้วกดเข้าไปดูผล

**ถ้าสำเร็จ** จะเห็น commit ใหม่ชื่อ *"อัปเดตข้อมูลคัดกรองอัตโนมัติ ..."* ในหน้า repo

**ถ้าไม่สำเร็จ** หน้าสรุปจะบอกสาเหตุ — กรณีที่เป็นไปได้มากที่สุดคือ Yahoo บล็อกเครื่องของ GitHub เหมือนที่บล็อก Streamlit Cloud

> **ผมยังไม่ได้ทดสอบว่า Yahoo ยอมให้เครื่องของ GitHub ดึงไหม** เพราะทดสอบจากที่นี่ไม่ได้
> ถ้าโดนบล็อก ทางแก้คือกลับไปรันบน MacBook ด้วยมือ ซึ่งใช้ได้แน่นอนอยู่แล้ว

### ปรับเวลา

แก้บรรทัด `cron` ในไฟล์ `.github/workflows/update-snapshot.yml`
เวลาเป็น **UTC** ต้องลบ 7 ชั่วโมงจากเวลาไทย

| อยากให้รันเวลาไทย | ใส่ cron |
|---|---|
| 17:30 น. จ–ศ (ค่าปัจจุบัน) | `30 10 * * 1-5` |
| 08:00 น. ทุกวัน | `0 1 * * *` |
| 20:00 น. จ–ศ | `0 13 * * 1-5` |

> GitHub อาจรันช้ากว่าเวลาที่ตั้งไว้ 5–20 นาทีในช่วงที่คนใช้เยอะ ถือเป็นเรื่องปกติ

### รันบน Mac ด้วยมือ (เมื่อต้องการข้อมูลสดทันที)

ยังใช้ได้เหมือนเดิม และแนะนำให้ใช้ก่อนตัดสินใจลงทุนจริง
