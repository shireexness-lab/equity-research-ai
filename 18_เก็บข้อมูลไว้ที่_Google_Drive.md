# เก็บผลคัดกรองไว้ที่ Google Drive

> **ไม่ทำก็ได้** — ระบบทำงานได้ปกติโดยไม่ต้องตั้งค่านี้
> ทำแล้วได้อะไร : กดคัดกรองหุ้นไทยทั้งตลาดครั้งเดียว ครั้งต่อ ๆ ไปได้ผลใน 2 วินาที
> แม้จะปิดเว็บไปแล้วเปิดใหม่ หรือ Streamlit หลับไปแล้วตื่นขึ้นมา

---

## ทำไมต้องมีที่เก็บนอกเครื่อง

| สถานการณ์ | เก็บในเครื่องอย่างเดียว | เก็บที่ Google Drive ด้วย |
|---|---|---|
| กดคัดกรองซ้ำในหน้าเดิม | เร็ว ✅ | เร็ว ✅ |
| ปิดแท็บแล้วเปิดใหม่ | เร็ว ✅ | เร็ว ✅ |
| ไม่ได้เข้าเว็บ 30 นาที (Streamlit หลับ) | **ต้องดึงใหม่ 8 นาที** ❌ | เร็ว ✅ |
| เปิดจาก iPhone ทั้งที่เพิ่งดึงบน Mac | **ต้องดึงใหม่** ❌ | เร็ว ✅ |

Streamlit Community Cloud (แบบฟรี) จะปิดเครื่องเมื่อไม่มีคนใช้
พอตื่นขึ้นมาคือ **เครื่องใหม่** ไฟล์ที่เขียนไว้หายหมด — นี่คือปัญหาที่ Google Drive แก้ให้

---

## เก็บอะไรบ้าง (ตามที่อาจารย์สั่ง — เฉพาะตัวเลขที่ใช้คำนวณ)

เก็บเฉพาะคอลัมน์ในตารางคัดกรอง:

```
ticker · ชื่อบริษัท · กลุ่ม · สกุลเงิน · ราคา · มูลค่าตลาด
P/E · P/BV · P/S · EV/EBITDA · ROE · อัตรากำไรขั้นต้น · อัตรากำไรสุทธิ
D/E · ปันผล · FCF Yield · โตรายได้
```

**ไม่เก็บ** งบการเงินย้อนหลัง ราคารายวัน หรือรายละเอียดบริษัท

| ชุดข้อมูล | จำนวนแถว | ขนาดจริง |
|---|---|---|
| หุ้นไทยทั้งตลาด | 866 | ~60 KB |
| หุ้นสหรัฐทั้งตลาด | ~10,000 | ~700 KB |

Google Drive ให้ฟรี 15 GB — ใช้ไปไม่ถึงหนึ่งในหมื่น

---

## ขั้นตอนตั้งค่า (ทำครั้งเดียว ~15 นาที)

### ขั้นที่ 1 — สร้างโปรเจกต์ Google Cloud

1. เปิด https://console.cloud.google.com/
2. ด้านบนซ้าย กดที่ชื่อโปรเจกต์ → **NEW PROJECT**
3. ตั้งชื่อ `equity-research` → **CREATE**
4. รอสักครู่ แล้วเลือกโปรเจกต์นี้ให้เป็นโปรเจกต์ปัจจุบัน

### ขั้นที่ 2 — เปิดใช้ Google Drive API

1. ในช่องค้นหาด้านบน พิมพ์ `Google Drive API`
2. กดเข้าไป → กดปุ่ม **ENABLE**

### ขั้นที่ 3 — สร้าง "บัญชีหุ่นยนต์" (Service Account)

> Service Account คือบัญชีสำหรับให้โปรแกรมใช้ ไม่ใช่บัญชีคน
> เราไม่เอาบัญชี Gmail ส่วนตัวไปให้โปรแกรมใช้ เพราะถ้ากุญแจหลุด
> คนอื่นจะเข้าถึงอีเมลและไฟล์ทั้งหมดของอาจารย์ได้

1. เมนูซ้าย → **APIs & Services** → **Credentials**
2. ด้านบน → **CREATE CREDENTIALS** → **Service account**
3. ชื่อ: `equity-app` → **CREATE AND CONTINUE** → **DONE**
   (ข้ามส่วน role และ user access ได้ ไม่ต้องกรอก)

### ขั้นที่ 4 — ดาวน์โหลดกุญแจ

1. ในหน้า Credentials กดที่ชื่อ service account ที่เพิ่งสร้าง
2. แท็บ **KEYS** → **ADD KEY** → **Create new key**
3. เลือก **JSON** → **CREATE**
4. ไฟล์ `.json` จะดาวน์โหลดลงเครื่อง — **นี่คือกุญแจ อย่าให้ใคร อย่าอัปขึ้น GitHub**

### ขั้นที่ 5 — เปิดโฟลเดอร์ให้หุ่นยนต์เข้าถึงได้

1. เปิดไฟล์ JSON ที่ดาวน์โหลดมา หา `"client_email"`
   จะได้อีเมลหน้าตาแบบ `equity-app@equity-research-xxxxx.iam.gserviceaccount.com`
2. เปิด https://drive.google.com/ → สร้างโฟลเดอร์ชื่อ `EquityResearchAI`
3. คลิกขวาที่โฟลเดอร์ → **แชร์** → วางอีเมลจากข้อ 1 → สิทธิ์ **ผู้แก้ไข (Editor)** → **ส่ง**
4. เปิดโฟลเดอร์นั้น แล้วดู URL:
   `https://drive.google.com/drive/folders/1AbCdEfGh...`
   ส่วนที่อยู่หลัง `folders/` คือ **folder id** — คัดลอกเก็บไว้

---

## ขั้นที่ 6 — ใส่กุญแจเข้าโปรแกรม

### กรณีรันบน MacBook

```bash
eq
```

วางไฟล์ JSON ที่ดาวน์โหลดมาลงในโฟลเดอร์โปรเจกต์ แล้วเปลี่ยนชื่อเป็น
`gdrive_service_account.json` :

```bash
mv ~/Downloads/equity-research-*.json ./gdrive_service_account.json
```

> `.gitignore` กันไฟล์นี้ไว้แล้ว จะไม่ถูกอัปขึ้น GitHub

ทดสอบ:

```bash
python3 snapshot.py --test
```

ควรได้:

```
  กุญแจ      : พบแล้ว
  เชื่อมต่อ   : สำเร็จ
  เขียนไฟล์  : สำเร็จ
  อ่านกลับ   : สำเร็จ
```

### กรณีเว็บที่ deploy แล้ว (Streamlit Cloud)

1. เข้า https://share.streamlit.io/ → เลือกแอป → **⋮** → **Settings** → **Secrets**
2. วางข้อความข้างล่างนี้ โดยแทนค่าจากไฟล์ JSON:

```toml
gdrive_folder_id = "1AbCdEfGh..."

[gdrive]
type = "service_account"
project_id = "equity-research-xxxxx"
private_key_id = "xxxxxxxx"
private_key = "-----BEGIN PRIVATE KEY-----\nMIIEvQ...\n-----END PRIVATE KEY-----\n"
client_email = "equity-app@equity-research-xxxxx.iam.gserviceaccount.com"
client_id = "1234567890"
token_uri = "https://oauth2.googleapis.com/token"
```

> **จุดที่พลาดกันบ่อยที่สุด** — `private_key` ต้องอยู่ในเครื่องหมายคำพูดเดียว
> และ `\n` ต้องเป็นตัวอักษร backslash กับ n สองตัวติดกัน **ไม่ใช่** การขึ้นบรรทัดจริง
> ให้คัดลอกจากไฟล์ JSON มาทั้งบรรทัดตรง ๆ จะถูกต้องอยู่แล้ว

3. **Save** → แอปจะรีสตาร์ทเอง

---

## วิธีใช้งาน

ในหน้า **คัดกรองทั้งตลาด** จะเห็นปุ่ม 2 ปุ่ม:

| ปุ่ม | ทำอะไร | ใช้เมื่อไร |
|---|---|---|
| **เริ่มคัดกรอง (ดึงข้อมูลใหม่)** | ดึงสด ๆ ใช้เวลา 3–10 นาที | ต้องการราคาล่าสุดของวันนี้ |
| **⚡ ใช้ข้อมูลที่บันทึกไว้** | อ่านจากที่เก็บ ได้ผลใน 2 วินาที | ทดลองปรับเกณฑ์คัดกรองไปมา |

บนปุ่ม ⚡ จะบอกว่าข้อมูลเก่ากี่ชั่วโมงแล้ว จะได้ตัดสินใจเองว่าพอใช้ไหม

**ข้อควรระวัง** — ข้อมูลที่บันทึกไว้คือ *ภาพนิ่งของวันที่ดึง* ราคาหุ้นเปลี่ยนทุกวัน
ถ้าจะตัดสินใจลงทุนจริง ให้กดดึงใหม่เสมอ ปุ่ม ⚡ เหมาะกับตอนสำรวจและปรับเกณฑ์

---

## ตรวจสอบว่ามีอะไรเก็บไว้บ้าง

```bash
python3 snapshot.py --list
```

```
ชุดข้อมูล                    แถว   อายุ (ชม.)   ขนาด (KB)
--------------------------------------------------------
thai-ทั้งหมด-ทุกกลุ่ม-866      866         2.4        61.3
us-all                     9,847        18.1       712.0

Google Drive : ตั้งค่าแล้ว
```

---

## ถ้าไม่อยากยุ่งกับ Google Cloud

ข้ามทั้งหมดนี้ได้เลย ระบบจะเก็บไว้ในเครื่องอย่างเดียว ซึ่งยังช่วยได้มากตอนรันบน MacBook
เสียแค่กรณีที่ Streamlit Cloud หลับแล้วตื่น ต้องดึงใหม่รอบหนึ่ง
