# 23 — โมเดลคลาสสิก 4 ตัว + การจัดเรียงตาราง

บันทึกข้อสรุปและการตัดสินใจที่ใช้อ้างอิงต่อได้ ไม่ใช่คู่มือการใช้งาน

---

## 1. โมเดลมาตรฐานสากล 4 ตัว (`distress.py`)

| โมเดล | บอกอะไร | เกณฑ์ที่ใช้ |
|---|---|---|
| **Altman Z** | ความเสี่ยงล้มละลาย | Z'' ตลาดเกิดใหม่ : ปลอดภัย > 5.85 · เฝ้าระวัง 4.15–5.85 · เสี่ยง < 4.15 |
| **Piotroski F** | คุณภาพงบ 9 ข้อ | 8–9 = แข็งแรง · 0–2 = อ่อนแอ |
| **Beneish M** | สัญญาณตกแต่งบัญชี | > −1.78 = ควรไปอ่านหมายเหตุงบ |
| **Ohlson O** | ความน่าจะเป็นล้มละลาย | ออกมาเป็น % โดยตรง |

### การตัดสินใจเชิงออกแบบ 3 ข้อ

**1. ปฏิเสธเมื่อโมเดลใช้กับกิจการนั้นไม่ได้**
ธนาคาร ประกัน REIT ถูกกันออกจาก Altman / Beneish / Ohlson เพราะงบสถาบันการเงิน
มีหนี้สิน ~90% ของสินทรัพย์เป็นเรื่องปกติ (เงินฝากลูกค้า) ถ้าใส่เข้าสูตรจะสรุปว่า
ธนาคารทุกแห่งกำลังจะล้ม — Piotroski ยังใช้ได้แต่ลดจำนวนข้อที่วัดได้

**2. ขาดข้อมูล = ไม่คำนวณ และระบุว่าขาดบรรทัดไหน**
ห้ามเติมศูนย์แทนค่าที่หาไม่เจอ เพราะกำไรสะสม = 0 จะทำให้ Z-Score ต่ำผิดปกติ
Piotroski ใช้ `ok=None` แปลว่า "วัดไม่ได้" — ไม่นับเป็นได้คะแนน และไม่นับเป็นตก

**3. เตือนเมื่อโมเดลขัดแย้งกัน**
ตัวอย่างจริง **CPALL** : Altman 4.06 (เสี่ยงสูง) · Piotroski 8/9 · Ohlson 4.8%
เกิดกับบริษัทที่กู้มาซื้อกิจการ (Makro, Lotus) ซึ่ง Altman ให้น้ำหนักหนี้สินสูงเป็นพิเศษ
ถ้าแสดงแต่คำเตือนของ Altman ผู้ใช้จะเข้าใจว่าบริษัทกำลังจะล้ม ซึ่งข้อมูลไม่ได้บอกอย่างนั้น

### ความครอบคลุมที่วัดได้จริง (หุ้นไทย)

Altman 98% · Piotroski 97% · Beneish 96% · Ohlson 98% ของตัวที่ใช้ได้ตามทฤษฎี

### บั๊กที่เจอระหว่างทาง — EDGAR ไม่ได้ดึงกำไรสะสม

หุ้นสหรัฐขาด `Retained Earnings` **67%** (หุ้นไทยขาด 0%) ทำให้ Altman ใช้ได้แค่ 33%
แก้โดยเพิ่ม `RetainedEarningsAccumulatedDeficit` ใน `BALANCE_TAGS` ของ `edgar_layer.py`

> **ผลจะดีขึ้นเฉพาะหุ้นที่ดึงข้อมูลใหม่** ตัวที่อยู่ในแคชเดิมยังขาดเหมือนเดิม

### ข้อจำกัดที่ต้องเขียนกำกับไว้ทุกที่

โมเดลเหล่านี้บอก **รูปแบบทางสถิติ** ไม่ได้บอกอนาคต
Beneish ที่ขึ้นเตือนไม่ได้แปลว่าโกง — บริษัทโตเร็วมากก็เข้าเกณฑ์ได้
ใช้เป็น **ธงให้ไปอ่านหมายเหตุงบ** ไม่ใช่ข้อสรุป

---

## 2. การจัดเรียงตารางใน Streamlit

### ปัญหาแกนกลาง

ลิงก์ `<a href="?sort=...">` ทำให้เบราว์เซอร์ **โหลดหน้าใหม่ทั้งหน้า**
= Streamlit เริ่ม session ใหม่ = ลืมว่าผู้ใช้อยู่หน้าไหน เลือกตลาดอะไร กรองอะไรไว้

### ทางแก้ที่ใช้ — ให้ลิงก์พกบริบทกลับมาด้วย

```
?sort=<คอลัมน์>&order=asc&ns=<ชื่อชุด>&m=<โหมด>&mk=<ตลาด>&cg=<กลุ่ม>
```

ตัวอ่าน query param ที่หัวไฟล์ `app.py` ตั้งค่าเหล่านี้คืน **ก่อนหน้าจะวาด**
ผู้ใช้จึงอยู่ที่เดิมทุกอย่าง

| พารามิเตอร์ | หน้าที่ |
|---|---|
| `ns` | ชื่อชุดการเรียง — แยก session key ไม่ให้ไปทับการเรียงของหน้าอื่น |
| `m` | โหมด/หน้า — ถ้าไม่ส่ง จะเด้งกลับหน้าแรกทุกครั้งที่กดเรียง |
| `mk` · `cg` | ตลาดและกลุ่มที่เลือกไว้ |

**เงื่อนไขที่ต้องรักษา** — widget ที่ต้องจำค่าข้ามการโหลดใหม่ ต้องตั้ง `index=` จาก
session ไม่ใช่ปล่อยให้ default (Streamlit จะไม่สนใจ `value=` ถ้า widget มี key อยู่แล้ว
แต่ session ใหม่ยังไม่มี key จึงใช้ `index=` ได้)

### ให้ทางเลือกสองแบบเสมอ

| วิธี | ข้อดี | ข้อเสีย |
|---|---|---|
| กดหัวตาราง | เร็ว คุ้นเคย | โหลดหน้าใหม่ · บนมือถือต้องเลื่อนไปหาหัวคอลัมน์ |
| ช่องเลือก (selectbox + radio) | ไม่โหลดหน้าใหม่ · กดง่ายบนมือถือ | กินพื้นที่จอ |

### กฎการเรียง 3 ข้อ

**1. คอลัมน์ตัวเลขต้องเรียงด้วยค่า ไม่ใช่ตัวอักษร**
ไม่งั้น 100 จะมาก่อน 9 — ตรวจด้วย `pd.to_numeric(...).notna().sum() >= len/2`

**2. ช่องคำแนะนำต้องเรียงตามระดับ ไม่ใช่ตัวอักษร**
เรียงตามตัวอักษรจะได้ Accumulate · Buy · Hold · Reduce · Sell · **Strong Buy**
คือ Strong Buy ไปอยู่ท้ายสุดติดกับ Sell ซึ่งอ่านแล้วเข้าใจผิดทันที
ใช้ลำดับจาก `tools_deep_scan.ORDER` แบบกลับด้าน ให้ "มาก → น้อย" = ดีที่สุดขึ้นก่อน

**3. คอลัมน์ตั้งต้นต้องมีข้อมูลจริงในกลุ่มที่เลือกอยู่**
กลุ่ม "เข้าใหม่" ไม่มีค่าในช่องส่วนลดขยับเลย (ไม่มีของเดิมให้เทียบ)
ถ้าตั้งเป็นคอลัมน์ตั้งต้น กดเรียงแล้วตารางจะนิ่งสนิทเหมือนปุ่มเสีย

---

## 3. บทเรียนที่ใช้ซ้ำได้

**อ้างชื่อโหมดด้วยตัวแปร ไม่ใช่ข้อความดิบหรือหมายเลข**
เพิ่ม `M_BUY` ต่อจาก `M_ONE` `M_SCREEN` `M_DEEP` — เคยพังมาแล้วตอนใช้หมายเลขในรายการ
เพราะเพิ่มหน้าใหม่แล้วหมายเลขเลื่อน

**git push ถูกปฏิเสธ = ระบบอัตโนมัติส่งของขึ้นไปก่อน ไม่ใช่งานหาย**

```
git pull --rebase
git push
```

ตรวจก่อนได้ว่าจะชนกันไหม :

```
git fetch origin main
git diff --name-only HEAD...origin/main     # remote แก้ไฟล์ไหน
```

โดยปกติไม่ชน เพราะระบบอัตโนมัติแตะเฉพาะ `data/snapshots/` ส่วนงานที่ทำมือคือไฟล์ `.py`

---

## 4. สิ่งที่ยังค้าง

| เรื่อง | สถานะ |
|---|---|
| คอลัมน์ 4 โมเดลในผลวิเคราะห์ลึก | ว่างจนกว่าจะรันรอบใหม่ (`--refresh-days`) ทยอยเติมเองตามรอบ 3 วัน |
| Altman หุ้นสหรัฐ | ดีขึ้นเฉพาะตัวที่ดึงข้อมูลใหม่หลังแก้ `edgar_layer` |
| Strong Buy = 0 ทั้งสองตลาด | คะแนนสูงสุด 70.7 ไทย / 74.4 สหรัฐ เทียบเกณฑ์ 82 — ต้นเหตุคือปีงบสั้นทำให้คะแนนความน่าเชื่อถือตัน ทางออกคือรอคลังงบสะสมโตขึ้น (ดูไฟล์ 22) |
| โลโก้บริษัท | ดึงจากเว็บบริษัทได้ราว 60% ตัน ต้องใช้บริการเสียเงินถ้าอยากครบ — ยังไม่ตัดสินใจ |

---

*ตัวเลขทุกตัวในเอกสารนี้วัดจากข้อมูลจริงในเครื่อง ไม่ใช่การประมาณ*
*เอกสารนี้เป็นบันทึกทางเทคนิค ไม่ใช่คำแนะนำการลงทุน*
