# คู่มือ — อัปเดตผลวิเคราะห์ลึก (กดเอง + อัตโนมัติทุก 3 วัน)

เอกสารนี้อธิบายวิธีทำให้ผลวิเคราะห์ลึกใหม่อยู่เสมอ โดยไม่ต้องจำคำสั่ง

---

## 0. เข้าใจก่อน — ทำไมถึงต้องทยอยทำ

| | คัดกรองเร็ว | **วิเคราะห์ลึก** |
|---|---|---|
| เวลาต่อหุ้น 1 ตัว | 1.5 วินาที | **30 วินาที** (ช้ากว่า 20 เท่า) |
| ได้อะไร | P/E, D/E, ROE ฯลฯ | **คำแนะนำ Strong Buy…Sell** |
| หุ้นไทย 866 ตัว | 20 นาที | **7 ชั่วโมง** |
| หุ้นสหรัฐ 10,398 ตัว | 4 ชั่วโมง | **87 ชั่วโมง** |

87 ชั่วโมงคือ 3 วันครึ่งแบบไม่หยุดพัก ระบบจึงออกแบบเป็น **ทยอยทำทีละรอบ**

หลักการสำคัญ 3 ข้อ

1. **ตัวที่ทำแล้วจะไม่ทำซ้ำ** — กดกี่ครั้งก็ทำต่อจากจุดที่ค้าง ไม่เริ่มใหม่
2. **หยุดกลางคันได้** — ระบบบันทึกทุก 10 ตัว ปิดเครื่องแล้วรันใหม่ได้เลย
3. **แต่ละรอบแบ่ง 70 / 30** — 70% ทำตัวใหม่ที่ยังไม่เคยแตะ · 30% อัปเดตตัวเก่า

> ข้อ 3 สำคัญกว่าที่คิด ถ้าไม่แบ่งไว้ กว่าจะครบหุ้นสหรัฐหมื่นตัวจะใช้เวลา
> 3 เดือนครึ่ง และตลอด 3 เดือนนั้นจะไม่มีรายงาน "สิ่งที่เปลี่ยนแปลง" ให้ดูเลย
> การกันไว้ 30% ทำให้ครอบคลุมช้าลงเป็น 5 เดือน แต่ได้เห็นความเคลื่อนไหวทุกรอบ

---

## 1. กดอัปเดตเอง (ในโปรแกรม)

**เปิดโปรแกรมบน MacBook** (ไม่ใช่บนเว็บ — เหตุผลอยู่ข้อ 4)

```
eq
streamlit run app.py
```

ไปที่โหมด **🏆 รายการ Strong Buy** → เลือกตลาด → กด **⚙️ อัปเดตผลวิเคราะห์**

### ตัวเลือกในแผงนั้น

| ตัวเลือก | ความหมาย | ใช้เมื่อไร |
|---|---|---|
| **ทั้งตลาด** | ทำต่อจากที่ค้าง ตัวที่เคยทำแล้วข้าม | ปกติใช้อันนี้ |
| **เฉพาะตัวที่ผลเก่าแล้ว** | ดึงเฉพาะตัวที่วิเคราะห์ไว้เกิน N วัน มาทำใหม่ | อยากรู้ว่าอะไรเปลี่ยน |
| **เฉพาะ 300 ตัวคะแนนสูง** | คัดกรองเร็วทั้งตลาดก่อน แล้วลึกเฉพาะหัวตาราง | อยากได้ผลเร็ว |
| **หยุดเองเมื่อครบกี่ชั่วโมง** | หมดเวลาแล้วบันทึกและหยุด | ตั้ง 6 ชม. ถ้ารันตอนกลางคืน |
| **เสร็จแล้วส่งขึ้นเว็บ** | commit + push ให้เอง | เปิดไว้ จะได้ดูจากมือถือได้ |

### สิ่งที่เกิดขึ้นหลังกด

โปรแกรมจะ **เปิดงานแยกออกไปทำเบื้องหลัง** ไม่ใช่ทำในหน้าเว็บ ผลคือ

- หน้าเว็บไม่ค้าง กดดูอย่างอื่นได้ระหว่างรอ
- **ปิดแท็บเบราว์เซอร์ได้ งานเดินต่อ** กลับมาเปิดใหม่แล้วดูความคืบหน้าได้
- ปิด Streamlit ได้ งานก็ยังเดิน (เพราะแยกกลุ่มกระบวนการไว้แล้ว)

กด **⏹ หยุดงานนี้** ได้ทุกเมื่อ — ผลที่ทำไปแล้วไม่หาย

---

## 2. อัปเดตอัตโนมัติทุก 3 วัน

มีสองทางให้เลือก **ทำทั้งคู่ได้ ไม่ชนกัน** เพราะตัวที่ทำแล้วจะไม่ถูกทำซ้ำ

### ทาง ก — GitHub Actions (ไม่ต้องเปิดเครื่อง)

ตั้งไว้ให้แล้วที่ `.github/workflows/update-deep.yml` — push ขึ้นไปก็ทำงานเลย

| | |
|---|---|
| หุ้นไทย | วันที่ 1, 7, 13, 19, 25 เวลา 17:30 น. |
| หุ้นสหรัฐ | วันที่ 4, 10, 16, 22, 28 เวลา 17:30 น. |
| งบเวลา/รอบ | 5 ชั่วโมง (เพดานของ GitHub คือ 6) |

**ข้อควรระวัง** — Yahoo อาจปิดกั้นเครื่องของ GitHub ถ้าโดน ผลจะเป็นศูนย์
ดูได้ที่แท็บ **Actions** บน GitHub ระบบจะเขียนบอกไว้ชัดเจนว่าโดนหรือไม่

สั่งรันเองได้ที่ Actions → *วิเคราะห์ลึกอัตโนมัติทุก 3 วัน* → **Run workflow**

### ทาง ข — บน MacBook (ไม่โดน Yahoo ปิดกั้น)

ติดตั้งครั้งเดียว

```
eq
./setup_auto_deep.sh
```

หลังจากนั้นเครื่องจะทำเองทุก 3 วัน สลับตลาดไทย/สหรัฐ รอบละไม่เกิน 5 ชั่วโมง
แล้วส่งขึ้น GitHub ให้เอง

```
./setup_auto_deep.sh --สถานะ     ดูว่าติดตั้งไว้ไหม
./setup_auto_deep.sh --ถอน       เลิกทำอัตโนมัติ
./auto_deep.sh                   รันเดี๋ยวนี้เลยหนึ่งรอบ
```

> ถ้าถึงเวลาแล้วเครื่องหลับอยู่ macOS จะรันให้ทันทีที่ตื่น ไม่ข้ามรอบ
> แต่ถ้าปิดเครื่องสนิท จะรันรอบถัดไปตอนเปิดเครื่อง

**แนะนำ** — ใช้ทาง ข เป็นหลัก เพราะไม่โดนปิดกั้น ทาง ก ไว้เป็นตัวเสริม

---

## 3. อ่านรายงาน "สิ่งที่เปลี่ยนแปลง"

ทุกครั้งที่อัปเดตเสร็จ ระบบจะเทียบกับผลรอบก่อนแล้วสรุปให้ว่าอะไรขยับ
ดูได้ที่กล่อง **🔔 เปลี่ยนจากรอบก่อน** เหนือตารางหลัก

| สัญลักษณ์ | ความหมาย | ความสำคัญ |
|---|---|---|
| ⬆️ ดีขึ้น | คำแนะนำเลื่อนขึ้น เช่น Hold → Buy | **ดูก่อน** |
| ⬇️ แย่ลง | คำแนะนำเลื่อนลง | **ดูก่อน** |
| 💰 ถูกลง | ส่วนลดเพิ่มเกิน 10 จุด (ราคาลง หรือมูลค่าประเมินขึ้น) | รองลงมา |
| 💸 แพงขึ้น | ส่วนลดลดเกิน 10 จุด | รองลงมา |
| 🆕 | เพิ่งวิเคราะห์ได้ครั้งแรก | เพื่อทราบ |

ทำไมต้องขยับถึง **10 จุด** ถึงจะนับ — ต่ำกว่านั้นคือความผันผวนราคารายวันปกติ
ถ้านับหมดจะมีรายการหลายพันแถวทุกรอบจนหาของจริงไม่เจอ

ดูจาก Terminal ได้เหมือนกัน

```
python3 tools_deep_scan.py --changes
```

---

## 4. ทำไมกดอัปเดตจากเว็บบนมือถือไม่ได้

เว็บ Streamlit Cloud ทำงานบนเครื่องของศูนย์ข้อมูล ซึ่งมีปัญหาสองข้อ

1. **Yahoo ปิดกั้นหมายเลขเครื่องของศูนย์ข้อมูล** — ดึงข้อมูลไม่ได้ตั้งแต่แรก
2. **เครื่องถูกล้างทุกครั้งที่อัปเดตโค้ด** — งานที่รันค้าง 3 ชั่วโมงจะหายหมด

โปรแกรมจะตรวจให้เองว่าเปิดจากเครื่องไหน แล้วซ่อนปุ่มพร้อมอธิบายเหตุผล
ไม่ใช่ปุ่มกดแล้วเงียบหาย

---

## 5. คำสั่งทั้งหมด (สำหรับใช้จาก Terminal)

```bash
# ทำต่อจากที่ค้าง งบเวลา 6 ชั่วโมง ผลเก่าเกิน 3 วันทำใหม่
python3 tools_deep_scan.py --thai --all --hours 6 --refresh-days 3
python3 tools_deep_scan.py --us   --all --hours 6 --refresh-days 3

# เอาเฉพาะ 300 ตัวคะแนนพรีสกรีนสูงสุด (เร็วกว่า)
python3 tools_deep_scan.py --us --top 300

# ดูผลที่ทำไว้แล้ว / ดูสิ่งที่เปลี่ยน
python3 tools_deep_scan.py --show
python3 tools_deep_scan.py --changes

# เริ่มนับหนึ่งใหม่ (ย้ายผลเก่าไปเป็นไฟล์สำรอง ต้องพิมพ์ยืนยัน)
python3 tools_deep_scan.py --thai --reset
```

> `--reset` เคยลบไฟล์ทิ้งจริง ๆ และเคยทำให้ผลที่รันไป 854 ตัวหายทั้งหมด
> ตอนนี้แก้เป็น **ย้ายไปเก็บเป็นไฟล์สำรอง** และต้องพิมพ์คำว่า `ล้าง` ยืนยันก่อน
> ปกติไม่ต้องใช้เลย เพราะระบบทำต่อจากเดิมให้อยู่แล้ว

---

## 6. ตารางเวลาที่ควรคาดหวัง

| | หุ้นไทย 866 ตัว | หุ้นสหรัฐ 10,398 ตัว |
|---|---|---|
| ทำครบครั้งแรก | ราว 7 ชม. = 2 คืน | ราว 87 ชม. = **5 เดือน** ที่ 3 วัน/รอบ |
| อยากให้เร็วขึ้น | รันข้ามคืน `--hours 10` | รันข้ามคืนติดกันหลายคืน หรือลด `--refresh-days` |
| หลังครบแล้ว | อัปเดตทั้งตลาดทุก 3 วันได้สบาย | ทยอยอัปเดตวนไปเรื่อย ๆ |

**ข้อแนะนำตามจริง** — หุ้นสหรัฐไม่จำเป็นต้องครบหมื่นตัว ในนั้นมีทั้ง ETF
warrant และบริษัทเล็กมากที่ไม่มีงบให้วิเคราะห์ ระบบเรียงตามขนาดบริษัทให้แล้ว
ตัวที่มีความหมายจริงราว 2,000 ตัวแรก ซึ่งใช้เวลาราว 1 เดือน

---

*ผลจากระบบนี้เป็นการคำนวณเชิงกลไก ไม่ใช่คำแนะนำการลงทุน
ตัวเลขทุกตัวคำนวณด้วย Python และตรวจสอบย้อนได้*
