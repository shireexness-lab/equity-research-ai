# วิธีติดตั้งหน้าวัฏจักรเศรษฐกิจ (รอบ 1)

ทำตาม 4 ขั้นนี้เรียงกัน ใช้เวลาราว 5 นาที
**ยังไม่ต้องมี API key ใด ๆ** — รอบนี้ทำงานได้เลย

---

## ขั้นที่ 1 — เอาไฟล์เข้าโฟลเดอร์โปรเจกต์

ไฟล์ใหม่ 5 ไฟล์ ต้องอยู่ในโฟลเดอร์เดียวกับ `app.py` :

| ไฟล์ | ทำอะไร |
|---|---|
| `macro_data.py` | ดึงตัวเลขเศรษฐกิจจาก FRED และราคาตลาดจาก Yahoo |
| `cycle.py` | 5 กรอบวิเคราะห์ + การโหวต (ไม่มี AI แม้แต่บรรทัดเดียว) |
| `allocation.py` | แปลงช่วงวัฏจักรเป็นน้ำหนักพอร์ต + เทียบกับพอร์ตของคุณ |
| `cycle_page.py` | หน้าจอทั้งหมด + รายงาน PDF |
| `apply_cycle_patch.py` | ตัวช่วยแก้ `app.py` ให้อัตโนมัติ |

---

## ขั้นที่ 2 — เปิด Terminal แล้วเข้าโฟลเดอร์

เปิดโปรแกรม **Terminal** บน MacBook แล้วพิมพ์ (แก้ path ให้ตรงกับของคุณ) :

```
cd "/Users/shire/ClaudeCowork/Equity Research AI Pro"
```

ถ้าใช้ virtual environment อยู่ ให้เปิดใช้งานก่อน :

```
source .venv/bin/activate
```

---

## ขั้นที่ 3 — ทดสอบก่อนแก้ไฟล์จริง

พิมพ์ทีละบรรทัด รอให้บรรทัดก่อนหน้าเสร็จ

**3.1 ทดสอบตรรกะ (ไม่ต้องต่อเน็ต)**

```
python3 cycle.py --selftest
```

ต้องขึ้น `ผ่านทั้งหมด ✅` ที่บรรทัดสุดท้าย
ถ้าไม่ผ่าน **อย่าไปต่อ** — ส่งข้อความที่ขึ้นมาให้ผมดู

**3.2 ทดสอบการดึงข้อมูลจริง**

```
python3 macro_data.py --test
```

จะใช้เวลาราว 1–2 นาที เพราะดึงข้อมูล 33 ชุด
ผลที่ควรได้คือส่วนใหญ่ขึ้น `✓` และบรรทัดสรุปบอกว่าดึงสำเร็จกี่ชุด

> **ถ้ามีบางชุดขึ้น `✗` ไม่ต้องตกใจ** ระบบออกแบบมาให้ตัดชุดที่ใช้ไม่ได้
> ออกจากการโหวตแล้วทำงานต่อ พร้อมบอกบนหน้าเว็บว่าตัดอะไรออกและเพราะอะไร
> ที่ต้องกังวลคือกรณีที่ `✗` เกือบทั้งหมด ซึ่งแปลว่าเครือข่ายเข้า FRED ไม่ได้

---

## ขั้นที่ 4 — เพิ่มเมนูเข้า app.py

**4.1 ดูก่อนว่าจะแก้อะไรบ้าง (ยังไม่แก้จริง)**

```
python3 apply_cycle_patch.py
```

ต้องขึ้น `✓` ครบทั้ง 6 จุด แล้วลงท้ายว่า `ทุกจุดพร้อมแก้ ✅`

**4.2 แก้จริง**

```
python3 apply_cycle_patch.py --apply
```

สคริปต์จะ :
- สำรอง `app.py` เดิมไว้เป็น `app.py.ก่อนเพิ่มวัฏจักร.bak`
- แก้ 6 จุด
- **ตรวจว่าไฟล์ที่แก้แล้วยังเป็น Python ที่ถูกต้อง** ถ้าพังจะไม่เขียนทับเลย

**ถ้าอยากย้อนกลับ** เมื่อไรก็ได้ :

```
python3 apply_cycle_patch.py --undo
```

---

## ขั้นที่ 5 — เปิดเว็บ

```
streamlit run app.py
```

แล้วกดเมนูซ้าย → กลุ่ม **🌍 ภาพใหญ่** → **🌍 วัฏจักรเศรษฐกิจ**

---

# หน้าเว็บมีอะไรบ้าง

**ชั้นบน** นาฬิกาการลงทุนพร้อมเส้นทาง 24 เดือนย้อนหลัง · ช่วงปัจจุบัน ·
คะแนนเห็นพ้อง · ปรับพอร์ตได้กี่ %

**ชั้นกลาง** 4 แท็บย่อย

- *5 กรอบโหวตยังไง* — เห็นเลยว่ากรอบไหนเห็นด้วย (✅) กรอบไหนค้าน (⚠️)
- *ตัวชี้วัดทุกตัว* — ค่าล่าสุด ทิศทาง 3 เดือน และ z-score เทียบตัวเอง 10 ปี
- *🇹🇭 ฝั่งไทย* — SET เทียบเส้น 200 วัน · ค่าเงินบาท
- *สุขภาพข้อมูล* — ชุดไหนสด ชุดไหนตาย ถูกตัดออกเพราะอะไร

**ชั้นล่าง** ช่องกรอกพอร์ต → ตารางส่วนต่าง → กลุ่มอุตสาหกรรมที่ควรเน้น/เลี่ยง
→ ปุ่มสร้าง **รายงาน PDF**

---

# สิ่งที่ต้องรู้เกี่ยวกับรอบ 1

**สิ่งที่ทำได้จริงแล้ว**

- 5 กรอบวิเคราะห์ครบ ทำงานจากข้อมูลจริงทั้งหมด
- ตัดข้อมูลที่ค้างออกจากการโหวตอัตโนมัติ พร้อมรายงานว่าตัดอะไร
- กติกากันการปรับพอร์ตถี่เกินไป (ตัวคูณตามคะแนนเห็นพ้อง · เกณฑ์ 3% · เตือน 30 วัน)
- รายงาน PDF

**สิ่งที่ยังไม่ได้ทำ — จะทำในรอบถัดไป**

| หัวข้อ | สถานะ |
|---|---|
| น้ำหนักพอร์ตจาก backtest จริง | ตอนนี้ยังเป็น **ค่าตั้งต้นเชิงทฤษฎี** และเขียนกำกับไว้บนหน้าเว็บแล้ว |
| ตัวเลขเศรษฐกิจไทยจริง (ธปท.) | ตอนนี้ฝั่งไทยอ่านจากราคาตลาดล้วน |
| อัปเดตอัตโนมัติทุก 3 วัน | ยังต้องกดปุ่ม 🔄 เอง |
| แจ้งเตือนอีเมล / Telegram | ยังไม่มี |

**FRED API key** — ยังไม่ต้องมีก็ใช้ได้ แต่ถ้าใส่จะเสถียรขึ้น
เมื่อพร้อม บอกผมได้ ผมจะสอนสมัครทีละขั้น (ฟรี ใช้เวลา 2 นาที)
ใส่แล้วโปรแกรมจะสลับไปใช้เองอัตโนมัติ ไม่ต้องแก้โค้ดอะไรเลย

---

# ถ้าติดปัญหา

| อาการ | ทำอย่างไร |
|---|---|
| `apply_cycle_patch.py` บอกว่าหาจุดที่จะแก้ไม่เจอ | ส่งข้อความทั้งหมดให้ผมดู แปลว่า `app.py` ต่างจากที่คาดไว้ |
| หน้าเว็บขึ้น "ดึงข้อมูลไม่สำเร็จ" | กด 🔄 ดึงข้อมูลใหม่อีกครั้งในอีก 2-3 นาที มักเป็นปัญหาเครือข่ายชั่วคราว |
| กราฟขึ้นเป็นสี่เหลี่ยมแทนตัวอักษรไทย | แปลว่าหาฟอนต์ในโฟลเดอร์ `fonts/` ไม่เจอ บอกผมได้ |
| สร้าง PDF ไม่สำเร็จ | บอกผมพร้อมข้อความที่ขึ้น น่าจะเป็นเรื่องฟอนต์ใน WeasyPrint |
| อยากย้อนทุกอย่างกลับ | `python3 apply_cycle_patch.py --undo` แล้วลบไฟล์ใหม่ 5 ไฟล์ทิ้ง |
