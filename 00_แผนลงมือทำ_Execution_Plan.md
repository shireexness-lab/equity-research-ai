# Equity Research AI Pro — แผนลงมือทำจริง (Execution Plan)

**เวอร์ชัน:** 1.0 · **วันที่:** 7 สิงหาคม 2026
**สำหรับ:** อาจารย์ชิรายุ (MacBook Air M5, Python ติดตั้งแล้ว, ใช้ Terminal เป็น)
**เป้าหมาย:** ระบบวิเคราะห์หุ้นที่เปิดใช้ได้จาก iPhone / Android / Mac ด้วยลิงก์เดียว ค่าใช้จ่ายเกือบ 0 บาท/เดือน

> เอกสารนี้ **ไม่ใช่** พิมพ์เขียว (blueprint) — พิมพ์เขียวบอกว่า "จะสร้างอะไร"
> เอกสารนี้บอกว่า **"ลงมือทำยังไง วันไหน พิมพ์คำสั่งอะไร และจะเจอปัญหาอะไรบ้าง"**

---

## ส่วนที่ 1 — สรุปการตัดสินใจทางเทคนิค (อ่าน 3 นาที)

| คำถาม | คำตอบ | เหตุผล |
|---|---|---|
| ใช้ภาษาอะไร | Python เท่านั้น | ไม่ต้องเรียน JavaScript/Swift เลย |
| ทำแอปมือถือไหม | **ไม่ทำ** — ทำเว็บแทน | แอป iOS ต้องจ่าย Apple 99 USD/ปี + รอ review; เว็บเปิดจาก Safari/Chrome ได้ทันที ฟรี และเพิ่มไอคอนลงหน้าจอมือถือได้ (Add to Home Screen) ดูเหมือนแอปจริง |
| เฟรมเวิร์กเว็บ | **Streamlit** | เขียน Python ล้วน ไม่ต้องรู้ HTML/CSS |
| โฮสต์ที่ไหน | **Streamlit Community Cloud** (ฟรี) | ฟรีจริง มีข้อจำกัดที่ต้องรู้ — ดูส่วนที่ 6 |
| ข้อมูลหุ้น | **yfinance** เป็นหลัก | ฟรี ไม่ต้องสมัคร ไม่ต้องมี API key |
| ทำ PDF ยังไง | HTML → PDF ด้วย **WeasyPrint** | รองรับฟอนต์ไทยดีที่สุด และจัดหน้าสวยกว่าไลบรารีอื่น |
| AI ใช้ตอนไหน | เฟส C (สัปดาห์ที่ 6+) | ยังไม่ต้องรีบ — ทำตัวเลขให้ถูกก่อน |

### กฎเหล็กที่ต้องท่องให้ขึ้นใจ
1. **Python คำนวณ / AI เล่าเรื่อง** — ห้าม AI ตอบตัวเลขการเงินดิบเด็ดขาด
2. **ทีละ Part** — เสร็จหนึ่ง ทดสอบหนึ่ง แล้วค่อยไปต่อ
3. **AAPL ก่อน หุ้นไทยทีหลัง** — ทดสอบทุกอย่างด้วย AAPL จนนิ่ง ค่อยลอง `PTT.BK`

---

## ส่วนที่ 2 — ไทม์ไลน์ 6 สัปดาห์ (ทำวันละ 1–2 ชม.)

| สัปดาห์ | ทำอะไร | Part | ผลลัพธ์ที่จับต้องได้ (Definition of Done) |
|---|---|---|---|
| **0** | เตรียมเครื่อง | — | พิมพ์ `python3 -c "import yfinance"` แล้วไม่ error |
| **1** | Data Layer + ระบบ cache | 1 | `python3 data_layer.py AAPL` แล้วเห็นงบการเงิน 4 ปี บนหน้าจอ |
| **2** | วิเคราะห์งบ + Ratio Engine | 2, 3 | ได้ตาราง ROE / Net Margin / D/E / FCF ย้อนหลัง 4 ปี ตรงกับที่เช็คในเว็บ |
| **3** | Valuation Engine (DCF) | 4 | รัน DCF แล้วได้มูลค่าต่อหุ้น 1 ตัวเลข + ตาราง sensitivity |
| **4** | Report Generator (PDF ไทย) | 15 | ได้ไฟล์ `AAPL_report.pdf` 10–15 หน้า มีกราฟ ฟอนต์ไทยไม่กลายเป็นสี่เหลี่ยม |
| **5** | Web App + Deploy | 16, 17 | เปิดลิงก์จาก iPhone → พิมพ์ AAPL → กดปุ่ม → โหลด PDF ได้ |
| **6+** | ขยาย (Forecast / Bands / Technical / AI) | 5, 6, 8, 9, 11 | ทีละชิ้น ไม่มีกำหนด |

> **หมายเหตุสำคัญ:** ถ้าติดที่สัปดาห์ไหน **อย่าข้าม** ให้แก้ให้จบก่อน เพราะทุก Part ต่อยอดกัน

---

## ส่วนที่ 3 — โครงสร้างไฟล์ (สร้างตามนี้เป๊ะ ๆ)

```
~/equity-research-ai/
├── app.py                  ← Part 16 : หน้าเว็บ (ไฟล์ที่ Streamlit รัน)
├── data_layer.py           ← Part 1  : ดึงข้อมูล + cache
├── statements.py           ← Part 2  : YoY, CAGR, Common Size
├── ratios.py               ← Part 3  : 8 หมวด ratio
├── valuation.py            ← Part 4  : DCF, DDM, Relative
├── bands.py                ← Part 6  : ช่วงราคา Strong Buy…Sell
├── report.py               ← Part 15 : ประกอบ HTML → PDF
├── ai_layer.py             ← Part 9,11 : (สัปดาห์ 6+) เรียก Claude API
├── templates/
│   └── report.html         ← เทมเพลตรายงาน + CSS ฟอนต์ไทย
├── fonts/
│   ├── Sarabun-Regular.ttf
│   └── Sarabun-Bold.ttf
├── cache/                  ← เก็บข้อมูลที่ดึงมาแล้ว (กัน yfinance บล็อก)
├── output/                 ← PDF ที่สร้างเสร็จ
├── requirements.txt        ← รายชื่อไลบรารี Python
├── packages.txt            ← ไลบรารีระบบ (จำเป็นตอน deploy)
└── .gitignore              ← กันไม่ให้ cache/ และ secret หลุดขึ้น GitHub
```

**หลักการ:** 1 ไฟล์ = 1 Part = 1 หน้าที่ ถ้าไฟล์ไหนยาวเกิน ~300 บรรทัด แสดงว่าควรแตกเป็นไฟล์ย่อย

---

## ส่วนที่ 4 — สัปดาห์ที่ 0: เตรียมเครื่อง (ทำครั้งเดียว ~30 นาที)

เปิด **Terminal** แล้วพิมพ์ทีละบรรทัด กด Enter รอให้จบก่อนพิมพ์บรรทัดถัดไป

### 4.1 ตรวจว่ามีอะไรอยู่แล้วบ้าง
```bash
python3 --version
```
ต้องได้ `Python 3.11` ขึ้นไป — ถ้าต่ำกว่านั้นให้ติดตั้งใหม่จาก python.org

### 4.2 ติดตั้ง Homebrew (ตัวจัดการโปรแกรมของ Mac) — ถ้ายังไม่มี
```bash
brew --version
```
ถ้าขึ้น `command not found` ให้ติดตั้งตามคำสั่งในหน้า https://brew.sh

### 4.3 ติดตั้งไลบรารีระบบที่ WeasyPrint ต้องใช้ (**ขั้นตอนนี้คนมักลืม → PDF พังทั้งระบบ**)
```bash
brew install pango gdk-pixbuf libffi
```

### 4.4 สร้างโฟลเดอร์โครงการ + ห้องแยกของ Python (virtual environment)
```bash
mkdir -p ~/equity-research-ai
cd ~/equity-research-ai
python3 -m venv .venv
source .venv/bin/activate
```
> หลังจากนี้ **ทุกครั้งที่เปิด Terminal ใหม่** ต้องพิมพ์ 2 บรรทัดนี้ก่อนเสมอ:
> `cd ~/equity-research-ai` แล้ว `source .venv/bin/activate`
> ถ้าทำถูก จะเห็น `(.venv)` อยู่หน้าบรรทัด

### 4.5 ติดตั้งไลบรารี Python
```bash
pip install --upgrade pip
pip install yfinance pandas numpy matplotlib jinja2 weasyprint streamlit ta pyarrow
```

### 4.6 ทดสอบว่าใช้ได้จริง
```bash
python3 -c "import yfinance, weasyprint, streamlit; print('พร้อมแล้ว')"
```
ถ้าขึ้นคำว่า **พร้อมแล้ว** = จบสัปดาห์ที่ 0 ✅

---

## ส่วนที่ 5 — Definition of Done ของแต่ละ Part

ห้ามบอกว่า "เสร็จ" จนกว่าจะผ่านข้อทดสอบเหล่านี้

### Part 1 — Data Layer
- [ ] ดึง AAPL ได้ครบ 5 ก้อน: `info`, `income`, `balance`, `cashflow`, `prices`
- [ ] ดึงซ้ำครั้งที่ 2 **ไม่ยิงเน็ต** (อ่านจาก `cache/` แทน) — ตรวจโดยดูว่าเร็วขึ้นชัดเจน
- [ ] ลอง ticker ผิด เช่น `XXXXX` แล้วโปรแกรมแจ้งเตือนสวย ๆ ไม่ crash
- [ ] ลอง `PTT.BK` แล้วรู้ว่าได้งบกี่ปี (คาดว่า 4 ปี)

### Part 2 — Statement Analyzer
- [ ] YoY ของ Revenue AAPL ปีล่าสุด ตรงกับที่เปิดดูใน Yahoo Finance
- [ ] CAGR คำนวณจากจำนวนปีที่ถูก (4 จุดข้อมูล = 3 ปี ไม่ใช่ 4)
- [ ] Common size: ทุกบรรทัดหารด้วย Total Revenue แล้ว Revenue เอง = 100%

### Part 3 — Ratio Engine
- [ ] **เช็คมือ 3 ตัว**: เปิดเว็บ (Yahoo/StockAnalysis) เทียบ ROE, Net Margin, D/E ของ AAPL ต้องใกล้เคียงกัน (คลาดเคลื่อน <2% ถือว่าผ่าน เพราะนิยามอาจต่างกันเล็กน้อย)
- [ ] ถ้าตัวหารเป็น 0 หรือติดลบ → คืนค่า `None` ไม่ใช่ crash
- [ ] มีฟังก์ชัน "หาชื่อบรรทัดใกล้เคียง" เพราะ yfinance ตั้งชื่อไม่เหมือนกันทุกหุ้น

### Part 4 — Valuation
- [ ] DCF ให้ผลลัพธ์เป็นตัวเลขเดียว หน่วยเป็น USD/หุ้น
- [ ] ทดสอบตรรกะ: ถ้า `wacc` เพิ่ม → มูลค่าต้อง**ลด** / ถ้า `g1` เพิ่ม → มูลค่าต้อง**เพิ่ม**
- [ ] มีการกันพัง: ถ้า `g2 >= wacc` ให้แจ้ง error ชัดเจน (ไม่งั้นหารด้วยเลขติดลบ ได้มูลค่าประหลาด)
- [ ] ตาราง sensitivity 5×5 (wacc × g1)

### Part 15 — Report
- [ ] PDF เปิดด้วย Preview บน Mac ได้
- [ ] **ภาษาไทยไม่เป็นสี่เหลี่ยม** ⬛⬛⬛ (ถ้าเป็น = ฟอนต์ไม่ถูกฝัง)
- [ ] มีกราฟอย่างน้อย 3 รูป: Revenue trend, Margin trend, ราคาย้อนหลัง
- [ ] ท้ายรายงานมีวันที่สร้าง + แหล่งข้อมูล + **คำเตือนว่าไม่ใช่คำแนะนำการลงทุน**

### Part 16/17 — Web + Deploy
- [ ] เปิดจาก Safari บน iPhone แล้วหน้าจอไม่เพี้ยน
- [ ] ปุ่มดาวน์โหลด PDF ทำงานบนมือถือ
- [ ] ไม่มี API key อยู่ในโค้ดที่ push ขึ้น GitHub

---

## ส่วนที่ 6 — กับดัก 8 ข้อที่จะเจอแน่ ๆ (อ่านก่อนเจอ จะประหยัดเวลาเป็นวัน)

| # | ปัญหา | อาการ | ทางแก้ |
|---|---|---|---|
| 1 | **`pandas-ta` พังกับ numpy รุ่นใหม่** | `ImportError: cannot import name 'NaN' from 'numpy'` | **อย่าใช้ `pandas-ta`** ใช้ไลบรารี `ta` แทน (ติดตั้งไว้แล้วในข้อ 4.5) มี indicator ครบเหมือนกัน |
| 2 | **WeasyPrint หาไลบรารีไม่เจอ** | `cannot load library 'libgobject-2.0-0'` | ต้อง `brew install pango` ก่อน (ข้อ 4.3) และตอน deploy ต้องมีไฟล์ `packages.txt` |
| 3 | **ภาษาไทยใน PDF เป็นสี่เหลี่ยม** | ⬛⬛⬛ | โหลดฟอนต์ Sarabun (.ttf) จาก Google Fonts มาไว้ใน `fonts/` แล้วประกาศ `@font-face` ใน CSS ด้วย path แบบเต็ม |
| 4 | **yfinance โดนจำกัดการเรียก** | `Too Many Requests` / ได้ตารางว่าง | ใช้ cache ตั้งแต่ Part 1 (บังคับ) — ดึงครั้งเดียวเก็บเป็นไฟล์ `.parquet` ใช้ซ้ำได้ 24 ชม. |
| 5 | **ชื่อบรรทัดงบไม่ตรงกันแต่ละหุ้น** | `KeyError: 'Total Revenue'` | เขียนฟังก์ชัน `find_row(df, ["Total Revenue","Revenue","TotalRevenue"])` ลองหลายชื่อ ถ้าไม่เจอคืน `None` |
| 6 | **Streamlit Cloud ฟรี = repo ต้องเป็น public** | โค้ดเห็นได้ทั้งโลก | ยอมรับได้ถ้าไม่มีความลับ แต่ **ห้ามใส่ API key ลงโค้ด** ให้ใช้ `st.secrets` เท่านั้น (ตั้งค่าในหน้าเว็บ Streamlit ไม่ได้อยู่ใน GitHub) |
| 7 | **แอปหลับ + RAM แค่ ~1 GB** | เปิดครั้งแรกช้า / แอปดับกลางคัน | ปกติ — กดเปิดใหม่แล้วรอ ~30 วิ; อย่าโหลดข้อมูลหุ้นหลายสิบตัวพร้อมกัน |
| 8 | **หุ้นไทยข้อมูลตื้น** | งบมีแค่ 4 ปี, `info` ไม่ครบ | ยอมรับในเฟสแรก DCF 4 ปีก็ทำได้แต่ต้องระบุในรายงานว่าข้อมูลจำกัด — ถ้าจะลึกจริงค่อยเพิ่ม SETSMART ทีหลัง |

---

## ส่วนที่ 7 — งบประมาณจริง (ตรวจสอบ ณ สิงหาคม 2026)

| รายการ | ค่าใช้จ่าย | หมายเหตุ |
|---|---|---|
| Python + ไลบรารีทั้งหมด | **0 บาท** | โอเพนซอร์สทั้งหมด |
| yfinance (ข้อมูลหุ้น) | **0 บาท** | ไม่ต้องสมัคร |
| GitHub | **0 บาท** | repo แบบ public |
| Streamlit Community Cloud | **0 บาท** | 1 GB RAM, แอปหลับหลังไม่มีคนใช้ 12 ชม. |
| **รวมเฟส A–B** | **0 บาท/เดือน** | ✅ ตรงเป้า "ถูกที่สุด" |
| Claude API (เฟส C, ทางเลือก) | Haiku 4.5 ≈ $1 เข้า / $5 ออก ต่อ 1 ล้าน token | รายงาน 1 ฉบับใช้ราว 15–25k token → **ประมาณ 1–3 บาท/รายงาน** |
| SETSMART (หุ้นไทยลึก, ทางเลือก) | ~250 บาท/เดือน | เอาไว้ทีหลัง อย่าเพิ่งจ่าย |

**สรุป:** ถ้าไม่แตะ Part 12–13 (News/Transcript AI) ค่าใช้จ่ายคือ **0 บาท** จนกว่าจะเปิด AI layer ซึ่งก็อยู่ในหลักไม่กี่บาทต่อรายงาน

---

## ส่วนที่ 8 — วิธีทำงานกับผมในแต่ละสัปดาห์

เพื่อให้ไม่หลง แนะนำให้เปิดบทสนทนาใหม่ต่อ 1 Part แล้วบอกผมแบบนี้:

> "สัปดาห์ที่ 1 — สร้าง Part 1 Data Layer ตามแผน เขียนไฟล์ `data_layer.py` ให้ครบพร้อม cache แล้วบอกวิธีทดสอบ"

ผมจะ:
1. เขียนไฟล์ให้ครบ พร้อมคอมเมนต์ภาษาไทยทุกบรรทัดสำคัญ
2. บอกคำสั่ง Terminal ที่ต้องพิมพ์ ทีละบรรทัด
3. บอกว่า "ถ้าเห็นแบบนี้ = ผ่าน / ถ้าเห็นแบบนี้ = ยังไม่ผ่าน แก้ยังไง"

**สิ่งที่อาจารย์ต้องทำ:** copy คำสั่งไปวางใน Terminal แล้วส่งผลลัพธ์ที่ได้กลับมาให้ผมดู (โดยเฉพาะข้อความ error — ส่งมาทั้งก้อนเลย)

---

## ส่วนที่ 9 — เช็คลิสต์ก่อนเริ่มสัปดาห์ที่ 1

- [ ] `brew install pango gdk-pixbuf libffi` ผ่าน
- [ ] สร้างโฟลเดอร์ `~/equity-research-ai` + venv แล้ว
- [ ] `python3 -c "import yfinance, weasyprint, streamlit; print('พร้อมแล้ว')"` ขึ้น "พร้อมแล้ว"
- [ ] สมัคร GitHub ไว้ (ยังไม่ต้องใช้ ใช้จริงสัปดาห์ที่ 5)
- [ ] โหลดฟอนต์ Sarabun จาก Google Fonts เก็บไว้ (ใช้จริงสัปดาห์ที่ 4)

---

## ⚠️ หมายเหตุกฎหมาย

ใช้ส่วนตัวไม่มีปัญหา แต่หาก **ให้บริการหรือขายคำแนะนำซื้อขายหุ้น** จะเข้าข่ายธุรกิจที่ ก.ล.ต. ไทยกำกับดูแล ควรปรึกษาผู้เชี่ยวชาญด้านกฎหมายก่อนทำเชิงพาณิชย์

ทุกรายงานที่ระบบสร้าง ควรมีข้อความท้ายเล่มว่า:
> *เอกสารนี้จัดทำโดยระบบอัตโนมัติเพื่อการศึกษาส่วนบุคคล ไม่ใช่คำแนะนำการลงทุน*

---

*เอกสารนี้เป็นแผนงานทางเทคนิค ราคา API และเงื่อนไขบริการอาจเปลี่ยนแปลง ตรวจสอบก่อนใช้จริงเสมอ*
