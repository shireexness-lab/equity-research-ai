# Part 17 — คู่มือขึ้นออนไลน์ (Deployment)

**เป้าหมาย:** ได้ลิงก์เดียวที่เปิดจาก iPhone / Android / Mac ที่ไหนก็ได้ **โดยไม่ต้องเปิด MacBook ทิ้งไว้**
**ค่าใช้จ่าย:** 0 บาท
**เวลาที่ใช้:** ประมาณ 30–40 นาที (ครั้งแรกครั้งเดียว)

---

## ⚠️ อ่านก่อนเริ่ม — เรื่องความเป็นส่วนตัว

Streamlit Community Cloud แบบฟรี **บังคับให้โค้ดอยู่ใน GitHub แบบสาธารณะ (public)**
แปลว่า **คนทั้งโลกเห็นโค้ดของอาจารย์ได้**

| สิ่งที่จะถูกเห็น | สิ่งที่จะไม่ถูกเห็น |
|---|---|
| ไฟล์ `.py` ทุกไฟล์ | อีเมล (เก็บใน Secrets ของ Streamlit) |
| ฟอนต์ Sarabun | ข้อมูล cache |
| `requirements.txt`, `packages.txt` | รายงาน PDF ที่เคยสร้าง |

ถ้ารับได้ → ทำต่อได้เลย
ถ้ารับไม่ได้ → ดู **ทางเลือกอื่น** ท้ายเอกสาร

**สิ่งที่ห้ามเด็ดขาด:** อย่าพิมพ์รหัสผ่านหรือ API key ลงในไฟล์ `.py` ใด ๆ

---

## ขั้นที่ 1 — เตรียมไฟล์ให้พร้อม (5 นาที)

```
eq
ls
```

ต้องเห็นไฟล์เหล่านี้ครบ:

```
app.py              data_layer.py     edgar_layer.py    statements.py
ratios.py           valuation.py      bands.py          report.py
requirements.txt    packages.txt      .gitignore
fonts/              .streamlit/config.toml
```

**ตรวจว่าฟอนต์อยู่ครบ** (สำคัญมาก — เซิร์ฟเวอร์เป็น Linux ไม่มีฟอนต์ไทยติดมา):
```
ls -lh fonts/
```
ต้องเห็น `Sarabun-Regular.ttf` และ `Sarabun-Bold.ttf` ขนาดราว 88 KB

> ฟอนต์ Sarabun ใช้สัญญาอนุญาต OFL ซึ่งอนุญาตให้แจกจ่ายต่อได้ จึงนำขึ้น GitHub ได้อย่างถูกต้อง

**ตรวจว่าไม่มีความลับหลุด:**
```
cat .gitignore
```
ต้องมีบรรทัด `.streamlit/secrets.toml` และ `cache/`

---

## ขั้นที่ 2 — สร้างบัญชี GitHub (5 นาที)

1. เปิด https://github.com → **Sign up**
2. ใช้อีเมลไหนก็ได้ ตั้งชื่อผู้ใช้ที่จำง่าย
3. ยืนยันอีเมล

---

## ขั้นที่ 3 — เตรียม Git บนเครื่อง (5 นาที)

```
git --version
```
ถ้ายังไม่มี macOS จะเด้งถามให้ติดตั้ง Command Line Tools → กดตกลง รอสักครู่

ตั้งชื่อและอีเมลผู้เขียนโค้ด (ทำครั้งเดียว):
```
git config --global user.name "Shire"
git config --global user.email "shireexness@gmail.com"
```

---

## ขั้นที่ 4 — อัปโหลดโค้ดขึ้น GitHub (10 นาที)

### 4.1 สร้างที่เก็บโค้ดบนเว็บ
1. ไปที่ https://github.com/new
2. **Repository name:** `equity-research-ai`
3. เลือก **Public**
4. **อย่าติ๊ก** "Add a README file" (เรามีไฟล์อยู่แล้ว)
5. กด **Create repository**
6. หน้าถัดไปจะแสดง URL แบบ `https://github.com/ชื่อผู้ใช้/equity-research-ai.git` — **คัดลอกเก็บไว้**

### 4.2 ส่งไฟล์ขึ้นไป

```
eq
git init
git add .
git status
```

**หยุดตรวจตรงนี้ก่อน** — ในรายการที่ขึ้นมา **ต้องไม่มี**:
- `cache/` หรือไฟล์ `.pkl`
- `output/` หรือไฟล์ `.pdf`
- `.venv/`
- `secrets.toml`

ถ้าเห็นรายการต้องห้ามข้างบน แปลว่า `.gitignore` ไม่ทำงาน **อย่าทำต่อ** ให้กลับมาถามผมก่อน

ถ้าสะอาดดีแล้ว:
```
git commit -m "Equity Research AI Pro - เฟส A"
git branch -M main
git remote add origin https://github.com/ชื่อผู้ใช้ของอาจารย์/equity-research-ai.git
git push -u origin main
```

> GitHub จะถามชื่อผู้ใช้และรหัสผ่าน — **ช่องรหัสผ่านต้องใส่ Personal Access Token ไม่ใช่รหัสผ่านปกติ**
> สร้างที่ https://github.com/settings/tokens → **Generate new token (classic)**
> → ติ๊กช่อง **repo** → กด Generate → คัดลอกมาวาง (จะเห็นครั้งเดียว เก็บไว้ให้ดี)

---

## ขั้นที่ 5 — Deploy ขึ้น Streamlit Cloud (10 นาที)

1. เปิด https://share.streamlit.io
2. **Sign in with GitHub** → กด Authorize
3. กด **Create app** → **Deploy a public app from GitHub**
4. กรอก:
   - **Repository:** `ชื่อผู้ใช้/equity-research-ai`
   - **Branch:** `main`
   - **Main file path:** `app.py`
5. กด **Advanced settings** → ช่อง **Secrets** ใส่บรรทัดนี้:
   ```
   SEC_CONTACT_EMAIL = "shireexness@gmail.com"
   ```
   > ส่วนนี้ **ไม่ขึ้น GitHub** เก็บอยู่ที่ Streamlit เท่านั้น
6. กด **Deploy**

รอ 3–8 นาที (ครั้งแรกช้าเพราะต้องติดตั้งไลบรารีทั้งหมด) เสร็จแล้วจะได้ลิงก์แบบ:
```
https://equity-research-ai-xxxxx.streamlit.app
```

---

## ขั้นที่ 6 — เพิ่มไอคอนลงหน้าจอ iPhone (2 นาที)

1. เปิดลิงก์ด้วย **Safari** บน iPhone
2. กดปุ่มแชร์ (สี่เหลี่ยมมีลูกศรขึ้น)
3. เลือก **เพิ่มไปยังโฮมสกรีน**
4. ตั้งชื่อ เช่น "หุ้น" → กด **เพิ่ม**

ได้ไอคอนบนหน้าจอที่กดเปิดได้เหมือนแอปจริง ๆ (Android ใช้ Chrome → เมนู 3 จุด → "เพิ่มไปยังหน้าจอหลัก")

---

## ขั้นที่ 7 — ทดสอบว่าใช้ได้จริง

| ข้อ | ต้องได้ |
|---|---|
| เปิดลิงก์จาก iPhone | หน้าเว็บโหลดขึ้น ไม่เพี้ยน |
| พิมพ์ `AAPL` → กดวิเคราะห์ | ได้ผลภายใน 40 วินาที ข้อมูล **15 ปี จาก SEC EDGAR** |
| กดสร้าง PDF → ดาวน์โหลด | เปิดไฟล์ได้ **ภาษาไทยไม่เป็นสี่เหลี่ยม** |
| พิมพ์ `PTT.BK` | ได้ผล 4 ปี จาก yfinance |
| ปิดแล้วเปิดใหม่พรุ่งนี้ | โหลดช้าครั้งแรก ~30 วิ (แอปตื่นจากหลับ) แล้วใช้ได้ปกติ |

**ข้อ 3 สำคัญที่สุด** — เป็นจุดที่พังบ่อยเพราะเซิร์ฟเวอร์เป็น Linux คนละระบบกับ Mac

---

## วิธีอัปเดตโค้ดในอนาคต

หลังแก้ไฟล์อะไรก็ตาม:
```
eq
git add .
git commit -m "อธิบายสั้น ๆ ว่าแก้อะไร"
git push
```
Streamlit Cloud จะ deploy ใหม่ให้เองภายใน 1–2 นาที **ไม่ต้องทำอะไรเพิ่ม**

---

## ข้อจำกัดของแพ็กเกจฟรี (ต้องรู้ไว้)

| ข้อจำกัด | ผลกระทบจริง | รับมืออย่างไร |
|---|---|---|
| หน่วยความจำ ~1 GB | วิเคราะห์หลายตัวพร้อมกันอาจทำให้แอปดับ | วิเคราะห์ทีละตัว |
| แอปหลับหลังไม่มีคนใช้ 12 ชม. | เปิดครั้งแรกของวันช้า ~30 วิ | ปกติ ไม่ต้องแก้ |
| repo ต้องเป็น public | โค้ดเปิดเผย | อย่าใส่ความลับในโค้ด |
| อัปเดตได้ไม่เกิน 5 ครั้ง/นาที | ถ้า push รัว ๆ จะถูกชะลอ | push ทีเดียวหลังแก้เสร็จ |

---

## ถ้าติดปัญหา

| อาการ | สาเหตุที่พบบ่อย | วิธีแก้ |
|---|---|---|
| `ModuleNotFoundError` ตอน deploy | ลืมใส่ไลบรารีใน `requirements.txt` | เพิ่มชื่อไลบรารี แล้ว push ใหม่ |
| PDF ภาษาไทยเป็นสี่เหลี่ยม | ฟอนต์ไม่ได้ขึ้น GitHub | `git add fonts/ -f` แล้ว push |
| `cannot load library libgobject` | `packages.txt` ไม่ถูกอ่าน | ตรวจว่าไฟล์อยู่ระดับบนสุดของ repo |
| แอปดับกลางคัน (Oh no) | ใช้หน่วยความจำเกิน 1 GB | ลดจำนวนปีที่ดึง หรือวิเคราะห์ทีละตัว |
| ดูปัญหาละเอียด | — | กด **Manage app** มุมขวาล่าง → อ่าน Logs |

---

## ทางเลือกอื่น (ถ้าไม่อยากให้โค้ดเป็นสาธารณะ)

| ทางเลือก | ค่าใช้จ่าย | หมายเหตุ |
|---|---|---|
| **ใช้บนเครื่องอย่างเดียว** | 0 บาท | `streamlit run app.py` — ไม่ต้อง GitHub เลย |
| **เปิดจากมือถือใน WiFi บ้าน** | 0 บาท | `streamlit run app.py --server.address 0.0.0.0` แล้วเปิด `http://<IP ของ Mac>:8501` (ต้องเปิด Mac ค้างไว้) |
| GitHub repo แบบ private + Streamlit เสียเงิน | มีค่าใช้จ่าย | ตรวจราคาปัจจุบันก่อน |
| เช่า VPS เอง | ~150–300 บาท/เดือน | ควบคุมได้เต็มที่ แต่ต้องดูแลเอง |

---

## ⚠️ หมายเหตุกฎหมาย

ใช้ส่วนตัวไม่มีปัญหา แต่ถ้า **เปิดให้คนอื่นใช้เพื่อประกอบการตัดสินใจลงทุน**
อาจเข้าข่ายการให้คำแนะนำการลงทุนที่ ก.ล.ต. ไทยกำกับดูแล

เมื่อ deploy แล้วลิงก์จะเป็นสาธารณะ ใครมีลิงก์ก็เข้าได้ **ควรปรึกษาผู้รู้ด้านกฎหมายก่อนเผยแพร่ลิงก์เป็นวงกว้าง**
ทุกหน้าของระบบมีข้อความกำกับว่าไม่ใช่คำแนะนำการลงทุนอยู่แล้ว แต่นั่นอาจไม่เพียงพอในเชิงกฎหมาย
